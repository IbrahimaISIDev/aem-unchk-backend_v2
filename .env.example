# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=islamic_platform
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=password
DATABASE_SYNCHRONIZE=true
DATABASE_LOGGING=true

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=1h
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production
JWT_REFRESH_EXPIRATION=7d

# Application Configuration
PORT=3000
NODE_ENV=development
API_PREFIX=api
CORS_ORIGIN=http://localhost:5173

# External APIs
ALADHAN_API_URL=https://api.aladhan.com/v1
ISLAMIC_CALENDAR_API_URL=https://api.islamicfinder.us/v1

# Redis Configuration (Optional)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# ==========================
# Email Configuration (Hybrid System with Fallback)
# ==========================

# SMTP configuration (Primary method - Gmail)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password-here
EMAIL_FROM="Your App Name" <your-email@gmail.com>

# Resend API (Fallback method - works in production when SMTP is blocked)
# Get your API key from https://resend.com/api-keys (Free tier: 3000 emails/month)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Frontend URL for password reset links
FRONTEND_URL=http://localhost:5173

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# Security
BCRYPT_ROUNDS=12

# Logging
LOG_LEVEL=debug

# --- Server ---
PORT=3000
API_PREFIX=api
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# --- Database (Dev) ---
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=islamic_platform_dev
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
# With current code, set to 'false' to enable TypeORM synchronize
DATABASE_SYNCHRONIZE=false
DATABASE_LOGGING=true

# --- JWT ---
JWT_SECRET=dev-secret-key
JWT_EXPIRATION=1h
JWT_REFRESH_SECRET=dev-refresh-secret
JWT_REFRESH_EXPIRATION=7d

# --- Finance Module ---
FINANCE_MODULE_ENABLED=true
FINANCE_EXPORT_MAX_RECORDS=10000
FINANCE_BACKUP_ENABLED=true
FINANCE_NOTIFICATION_EMAIL=finance@association.com
CONTRIBUTION_REMINDER_DAYS=7
CONTRIBUTION_DEFAULT_AMOUNT=500
EXPORT_TEMP_DIR=/tmp/exports
EXPORT_MAX_FILE_SIZE=10MB
ANALYTICS_CACHE_TTL=300
ANALYTICS_BATCH_SIZE=1000

# --- Email / Admin ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="AEM UNCHK" <noreply@aem-unchk.sn>
ADMIN_NOTIFICATION_EMAIL=
FRONTEND_URL=https://aem-unchk-connect.vercel.app